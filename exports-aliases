# the study directory is separated from the repo directory
export REPO_DIR="$HOME/bash_task_interface"
export STUDY_DIR="$HOME/study"

# repos bin dir should be exported to run key programs
export PATH=$REPO_DIR/bin:$PATH

# aliases for programs to show task number and create task directory
alias p="prompt"
alias r="createtaskdirs"
