# Resets the task directory to its initial state.
# A user can use this if the user has made changes to the file system but
# wants to start over.

rm -rf $TASK_DIR

mkdir $TASK_DIR
tar -xvzf $REPO_DIR/task_dir.tgz -C $TASK_DIR
